class Task {
  String title;
  String description;
  String priorityLevel;
  DateTime dueDate;

  Task({
    required this.title,
    required this.description,
    required this.priorityLevel,
    required this.dueDate,
  });
}
